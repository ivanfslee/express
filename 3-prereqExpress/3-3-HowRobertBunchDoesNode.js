//He uses VS code's integrated terminal
//And uses as few plugins as possible

//He'll run his node programs with nodemon
    //to refresh any changes to his program

    //run in terminal to install nodemon globally
        //npm install -g nodemon

//e.g. to run files with nodemon
    //run the following in terminal:
        //nodemon expressServer.js

//run the following in terminal to see versions:
    node -v //node js version

    npm -v //npm version

    nodemon -v //nodemon version